#include <bits/stdc++.h>
#include <string>
#include<iostream>
using namespace std;
int main() 
{
    string s;
    string str = "Amir";
    cout<<str<<endl;
    cout<<str.length()<<endl;     // using length function , we can find length of the string
    str.push_back('n');           // to add characters in the string
    cout<<str.length()<<endl;
    cout<<str;


return 0;
}